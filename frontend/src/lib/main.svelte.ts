export const fixtures: Record<string, Fixture> = $state({});
export const fixture_types: Record<string, FixtureType> = $state({});
export const fixture_modes: Record<string, FixtureMode> = $state({});

export const scenes: Record<string, Scene> = $state({});
export const sequences: Record<string, Sequence> = $state({});